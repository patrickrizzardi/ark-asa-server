# Code Review: m2-shared-economy-store Phase 5 (round 2 — delta re-review)

### Verdict: PASS

### Diff Scope
- Files changed (Phase 5 code, ignoring plan.md / state.md / notes.md churn): 7 — `entrypoint.sh`, `docker-compose.yml`, `Dockerfile`, `.gitignore`, `.env.test.example`, `.env.prod.example`, `README.md`
- Round-2 delta vs round-1: `entrypoint.sh` (helper extraction `_inject_mysql_block`, `|| exit 1` swallow-close, numeric `ARKSHOP_DB_PORT` guard, dead `MARIADB_HOST`/`MARIADB_PORT` tier dropped → literal defaults, password-mechanism comment corrected) + `docker-compose.yml` (auth-plugin `command:` removed from mariadb service)
- Phase commits reviewed: working tree vs BASE `4f19274` (Phase 5 uncommitted; chore commits 78d2639 + 148adca touch only plan.md/state.md and were excluded)

### What's Solid
- **The `|| exit 1` swallow-close is real and empirically proven.** I built a scratch rig: a failing command (jq stand-in) in the non-final position of the redirect+`||` chain, under `set -euo pipefail`. The handler is the list's final element, so `exit` fires unconditionally — the `mv` is never reached, and execution does NOT fall through. Exit code propagated (7 in my rig). The round-1 silent-swallow BLOCK is genuinely closed: a jq failure now hard-stops the boot with a FATAL line + `${cfg}` path instead of logging a false success against an unwritten config. The `rm -f "${tmp}"` in the handler also cleans the orphaned mktemp file on the failure path. Comment at lines 300-304 accurately describes the mechanism.
- **Helper extraction is provably behavior-preserving and a real DRY win.** `_inject_mysql_block` is called identically from both sites — line 366 (ArkShop) and line 374 (Permissions) — each passing only `${cfg}`; both read the same `ARKSHOP_DB_*` globals (correct: Permissions shares ArkShop's DB per plan finding + the inline comment). The jq filter is now single-sourced, so the two paths cannot drift. Round 1 had the swallow-risk duplicated in two jq blocks; the extraction closes it on BOTH paths in one place. The Permissions call stays gated behind `[[ -f "${perms_cfg}" ]] && jq -e 'has("Mysql")'`, so a Permissions config lacking a Mysql block is skipped — no phantom block injected there.
- **Numeric port guard added (line 354).** `[[ "${ARKSHOP_DB_PORT}" =~ ^[0-9]+$ ]]` runs before any jq, turning a raw `tonumber` failure into a clear FATAL naming the offending value. Defends the one place the filter builds a non-string JSON type.
- **Dead-tier removal is clean — round-1 Concern #1 resolved.** `MARIADB_HOST`/`MARIADB_PORT` (phantoms — defined nowhere in compose/env/passthrough) are gone repo-wide (grep confirms: only a notes.md changelog line survives, which is the correct home). HOST/PORT now default to literals `mariadb`/`3306`; NAME/USER/PASS correctly chain to the LIVE `MARIADB_DATABASE`/`MARIADB_USER`/`MARIADB_PASSWORD` peers. The asymmetry-smell the round-1 reviewer flagged is fixed: every fallback now points at a var that actually exists.
- **Comment fix lands — round-1 Concern #2 resolved.** Lines 326-330 now say the password is "passed to jq as a `--arg` command-line argument (transiently visible in /proc/<pid>/cmdline during the jq exec — acceptable in a single-user game container)." The wrong "env substitution" wording is gone and the argv exposure is now honestly documented. The FATAL guidance at lines 348-349 was also corrected to name only live vars (HOST→'mariadb', PORT→3306; NAME/USER/PASS→MARIADB_DATABASE/USER/PASSWORD) — an operator following it no longer gets pointed at a silently-ignored var.
- **Auth-plugin removal is the right call, no regression.** The `command: --default-authentication-plugin=mysql_native_password` line is fully gone from the mariadb service (only the unrelated `sysctl` `command:` remains). The coordinator's empirical justification holds: on MariaDB 11.4 the flag logs as a no-op MySQL-5.6/5.7-compat option, and a fresh mariadb:11.4 already creates the `arkshop` user as mysql_native_password. Removing dead config is correct per no-duct-tape. The mariadb service keeps its `MARIADB_*` env, healthcheck, volume, and logging intact — nothing else touched.
- **bash -n clean** on the full edited entrypoint; the credential path has no `eval` and no unquoted expansions.

### Required Fixes (BLOCK only — empty if PASS)
None — phase ready to commit.

### Concerns (non-blocking, but will bite later)
1. **[docker-compose.yml:73-74] Compose explicitly passes `ARKSHOP_DB_HOST`/`ARKSHOP_DB_PORT` with their own `:-mariadb`/`:-3306` defaults, so the entrypoint's identical `${ARKSHOP_DB_HOST:-mariadb}` fallback never fires in the compose path** — the env var is always set before the script sees it. This is belt-and-suspenders (the entrypoint defaults are the net for running the container OUTSIDE compose), not a bug, and it's a PRE-EXISTING structural choice unchanged this round (round 1 PASSed it). Not re-raising as new — noting only for completeness. No action.
2. **[entrypoint.sh:333-341] Round-1 Concern #3 (phantom `.Mysql` block on schema casing/nesting mismatch) remains a live static-evidence-ceiling item** — unchanged by this round's edits. The default ArkShop config ships inside the baked zip, so the schema casing still can't be byte-verified in this env. This is the dell-boot verifier's lane: a clean `ArkApi.log` (no `Singleton not found`, no DB error) + AC3 row-persist is what proves the casing is right; the failure symptom would be a silent file-backed shop, not a crash. Cross-flag → acceptance-verifier. No code change requested.

### Laziness Pattern Audit
- **Placeholder / mock pollution:** PASS — no dummy creds in code; the literal defaults (`mariadb`, `3306`, `arkshop`) are real service-name/port/db-name values with documented provenance, not placeholders.
- **Half-finished implementations:** PASS — the helper extraction handles the failure path (jq fail → `rm -f tmp` + FATAL + exit), the empty-cred path (fail-fast), the non-numeric-port path (new guard), the absent-config path (fail-fast), and the Permissions-no-Mysql path (skip). No happy-path-only gap introduced by the refactor.
- **Type escape hatches (code-quality angle):** N-A — bash, no type system; credential path verified `bash -n` clean, no unquoted expansion.
- **Smuggled TODOs (code-quality angle):** PASS — no TODO/FIXME/phase-ref introduced in the delta; the changelog note lives in notes.md, not code.
- **Magic constants without provenance:** PASS — `3306` is the standard MariaDB port (documented inline); `mariadb` is the compose service name; the `^[0-9]+$` port regex is self-evident. No new unexplained literal.
- **Documented deviations — adversarial inputs constructed:** N-A — this round's changes are direct fixes to round-1 reviewer Concerns (dead tier, comment, swallow, port guard) + an empirically-justified compose simplification (auth plugin). No NEW documented deviation introduced this round; round-1 deviations were validated at round 1 and are unchanged.

### Test Coverage Audit
N/A — not a bug-fix plan (Planned RED Repros section empty) and not a Tier-A correctness-critical unit. Surface is bash entrypoint + Docker compose infra; the "test" is the dell-boot runtime ACs (acceptance-verifier's lane with dell receipts). No existing test weakened — no test files in scope. The `|| exit 1` swallow-close was instead validated empirically via a scratch `set -e` rig (proof in What's Solid #1). Bug-fix Phase-1 failing test: N-A. Tier-A invariants: N-A.

### Bottom Line
Chief, the fixes landed clean — I rigged the `set -e` swallow and watched the `|| exit 1` abort fire exactly like the comment claims, the phantom MARIADB_HOST tier is gone repo-wide, and the auth-plugin line was dead config on 11.4 so dropping it is correct. Both round-1 Concerns resolved, zero regressions, PASS.
